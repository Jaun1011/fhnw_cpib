
program Fact()

global

    t:int32;

    fun factRec(n:int32) 
      returns const fact:int32
    do
      debugout n;
      if n = 0 then
        fact init := 1
      else
        fact init := n * factRec(n-1)
      endif
    endfun;


    fun factRec2(n:int32) 
      returns const fact:int32
    do
      debugout n;
      if n = 0 then
        fact init := 1
      else
        fact init := n * factRec(n-1)
      endif
    endfun
do
  t init := factRec(5); 
  debugout t


endprogram
